{
  "name": "obsidian-project-view",
  "version": "1.0.0",
  "description": "Obsidian plugin: project-based workspace with per-project panes, pinned notes and Google Drive sync.",
  "main": "main.js",
  "scripts": {
    "dev": "node esbuild.config.mjs",
    "build": "tsc -noEmit -skipLibCheck && node esbuild.config.mjs production"
  },
  "keywords": [
    "obsidian",
    "plugin",
    "projects"
  ],
  "author": "ngchenghow",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.11.0",
    "builtin-modules": "^3.3.0",
    "esbuild": "^0.20.2",
    "obsidian": "latest",
    "typescript": "^5.4.5"
  }
}
